# Skills Registry
# Single source of truth for AI skill metadata.
# Only list skills that have an actual prompt.md implementation.

skills:
  semantic-scan:
    path: scanning/semantic-scan
    prompt_file: prompt.md
    description: "AI security scan for business logic flaws, LLM risks, complex validation gaps"
    type: skill
    model_tier: heavy
    tools: [Read, Glob, Grep]
    supported_backends: [vertex, anthropic]
    estimated_tokens: { base: 30000, per_1k_loc: 500 }

  adversarial-review:
    path: scanning/adversarial-review
    prompt_file: prompt.md
    description: "Adversarial second-pass review: threat modeling, exploit path verification, false negative hunting"
    type: skill
    model_tier: heavy
    tools: [Read, Glob, Grep]
    supported_backends: [vertex, anthropic]
    estimated_tokens: { base: 40000, per_1k_loc: 600 }

  general-review:
    path: scanning/general-review
    prompt_file: prompt.md
    description: "Broad security review: injection, auth, crypto, data exposure, error handling, dependency risks"
    type: skill
    model_tier: heavy
    tools: [Read, Glob, Grep]
    supported_backends: [vertex, anthropic]
    estimated_tokens: { base: 35000, per_1k_loc: 550 }
